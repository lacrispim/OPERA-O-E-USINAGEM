
{
  "entities": {
    "OperatorProductionInput": {
      "title": "Operator Production Input",
      "description": "Represents a single production entry from an operator.",
      "type": "object",
      "properties": {
        "operatorId": {
          "type": "string",
          "description": "The ID of the operator."
        },
        "machineId": {
          "type": "string",
          "description": "The ID of the machine used."
        },
        "quantityProduced": {
          "type": "number",
          "description": "The number of units produced."
        },
        "productionTimeSeconds": {
          "type": "number",
          "description": "The time taken for production in seconds."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "The server timestamp of the entry."
        },
        "formsNumber": {
          "type": "string",
          "description": "Optional forms number associated with the production."
        },
        "factory": {
          "type": "string",
          "description": "The factory where the production took place."
        },
        "operationCount": {
          "type": "number",
          "description": "The number of operations performed."
        },
        "status": {
          "type": "string",
          "description": "The current status of the production entry.",
          "enum": ["Fila de produção", "Em produção", "Encerrado", "Rejeitado", "Enviado"]
        }
      },
      "required": ["operatorId", "machineId", "quantityProduced", "productionTimeSeconds", "timestamp", "factory", "status"]
    },
    "ProductionLossInput": {
      "title": "Production Loss Input",
      "description": "Represents a single entry for production loss.",
      "type": "object",
      "properties": {
        "operatorId": {
          "type": "string",
          "description": "The ID of the operator reporting the loss."
        },
        "factory": {
          "type": "string",
          "description": "The factory where the loss occurred."
        },
        "machineId": {
          "type": "string",
          "description": "The ID of the machine related to the loss."
        },
        "quantityLost": {
          "type": "number",
          "description": "The number of units lost."
        },
        "reason": {
          "type": "string",
          "description": "The reason for the production loss."
        },
        "timeLostMinutes": {
          "type": "number",
          "description": "The downtime in minutes due to the loss."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "The server timestamp of the loss entry."
        }
      },
      "required": ["operatorId", "factory", "machineId", "quantityLost", "reason", "timeLostMinutes", "timestamp"]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "/production-entries/{entryId}": {
      "schema": {
        "$ref": "#/entities/OperatorProductionInput"
      },
      "description": "Stores records of production entries from operators."
    },
    "/production-losses/{lossId}": {
      "schema": {
        "$ref": "#/entities/ProductionLossInput"
      },
      "description": "Stores records of production losses."
    }
  }
}
